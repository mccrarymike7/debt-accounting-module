# Copy to .env and adjust for your environment.
DATABASE_URL="postgresql://debt:debt@localhost:5432/debt_accounting?schema=public"
AUTH_SECRET="change-me"
AUTH_URL="http://localhost:3000"

# Optional: enrich PDF term extraction (falls back to heuristics without a key)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
